# Lovis development starter samples

Print-sample photographs used by `npm run seed:dev` as the first photo on
each seeded Lovis Printshop listing. Same set the supplier app shows on
Add a listing. Not used in production `npm run seed`.
